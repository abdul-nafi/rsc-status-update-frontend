import React from "react";

function UserDashboard() {
  return (
    <div>
      <h1>User Dashboard</h1>
      <p>This will show only the logged-in user's info later.</p>
    </div>
  );
}

export default UserDashboard;
